import 'package:flutter/material.dart';

class AppColors {
  // Transparent
  static const transparent = Color(0x00000000);

  // White
  static const white = Color(0xFFFFFFFF);

  // Black
  static const black = Color(0xFF000000);

  // Turquoise
  static const turquoise = Color(0xFF36F4EE);
}
